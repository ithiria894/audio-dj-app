# Deprecated

This file is superseded by the authoritative capture record:

**→ [`docs/compatibility/android-capture.md`](compatibility/android-capture.md)**

(The old version mixed a "lock test done" result with a "still-to-test: lock" list and over-claimed Android 16 as the "strictest" configuration. The compatibility doc has the calibrated, evidence-backed version.)
