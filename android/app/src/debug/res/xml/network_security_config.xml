<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG ONLY. Allows cleartext ws:// + http:// to the LAN LiveKit / token dev servers.
     Release builds do NOT include this (no src/release override) and stay TLS-only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.0.247</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
